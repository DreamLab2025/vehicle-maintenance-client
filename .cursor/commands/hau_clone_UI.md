---
description: Phân tích ảnh chụp UI → clone pixel-perfect bằng code. Data thay bằng API/props của dự án. Dùng khi chụp UI từ web/app khác muốn làm theo.
---

Kích hoạt **ĐỒNG THỜI** 2 skills:

1. **HAU CLONE UI EXPERT** từ `.cursor/skills/hau-clone-UI/SKILL.md`
   → Phân tích ảnh, clone layout, mapping data

2. **HAU NEXT UI EXPERT** từ `.cursor/skills/hau-next-UI/SKILL.md`
   → Chọn lib phù hợp, thêm animation, dark mode, responsive

<request>
$input
</request>

## Rules

- Phân tích ảnh theo 7 bước trong Clone SKILL.md
- Chọn component lib theo UI SKILL.md (Shadcn → Magic UI → Aceternity → HeroUI)
- Data trong ảnh → thay bằng props + mock data
- Output kèm bảng data mapping
- Code hoàn chỉnh, TypeScript, responsive, dark mode
- **Cấu trúc file**: đặt theo cấu trúc hiện tại của dự án
